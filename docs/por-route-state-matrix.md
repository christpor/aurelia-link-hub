# Manus POR Route and State Matrix — Aurelia Link Hub

| Item | Kind | Purpose | Discovery source | Status before repair | Evidence | Approved exclusion or next action |
|---|---|---|---|---|---|---|
| `/` | Route | Public link-in-bio hub and primary social destination. | Router and `Home.tsx`. | Implemented, pending final verification. | Local desktop/mobile visual checks; Vercel production alias returns HTML 200. | Reverify after media repair and redeployment. |
| `/404` and unmatched routes | Route/state | Recovery path for an unknown internal route. | App fallback router. | Implemented, unverified. | Fallback component exists in app router. | Direct-load and screenshot audit required. |
| `#top-links` | Navigation | Skip the campaign section and reach priority paths. | Skip link in `Home.tsx`. | Implemented, unverified. | Source target exists. | Keyboard activation audit required. |
| Campaign previous/next buttons | State | Switch manually between two featured campaign cards without timed motion. | `Home.tsx` campaign state. | Implemented, unverified. | Local source and initial visual review. | Exercise pointer and keyboard states after asset repair. |
| Campaign call to action | CTA | Send social visitor to the active campaign destination and log a local event. | Campaign data in `Home.tsx`. | Implemented, content pending. | `example.com` destination currently present. | Approved prototype limitation; replace with approved campaign URL before business launch. |
| Priority smart links | CTA | Present primary shop and journal destinations, each emitting a local event. | `SmartLink` calls in `Home.tsx`. | Implemented, content pending. | Demonstration destinations and event callback present. | Verify event drawer; replace destinations before business launch. |
| Shoppable product cards | CTA | Present selected product destinations with image, metadata, and price. | Product array in `Home.tsx`. | Implemented, final production verification pending. | Replacement public stock-image endpoints all returned successful HTTP responses and local desktop/mobile screenshots rendered the cards. | Verify the final Vercel media paths after redeployment. |
| Further-in link group | CTA | Present editorial, care, and appointment destinations. | `Home.tsx`. | Implemented, content pending. | Demonstration destinations present. | Approved prototype limitation; replace destinations before business launch. |
| Newsletter form | State | Validate an email field and show inline confirmation without backend submission. | `Home.tsx` form handler. | Implemented, unverified. | Native `type="email"`, visible label, and status area present. | Exercise empty, invalid, and valid states; provider integration remains out of scope. |
| Analytics preview drawer | State | Display recent local smart-link event payloads. | `Home.tsx` local `events` state. | Implemented, unverified. | Event drawer code present. | Exercise a representative click and confirm visual feedback. |
| Social links | CTA | Surface public social/email destinations. | Footer in `Home.tsx`. | Implemented, content pending. | Generic public URLs and prototype email address present. | Approved prototype limitation; replace before business launch. |
| Campaign/product/brand media | State | Preserve campaign hierarchy and commerce-card clarity. | Image and logo references in `Home.tsx` and `index.html`. | Implemented, final production verification pending. | All `/manus-storage` paths were removed from source; four public stock-image URLs passed endpoint checks; the mark is now CSS-drawn and the favicon is embedded. | Verify every visible media URL through the redeployed Vercel alias. |
